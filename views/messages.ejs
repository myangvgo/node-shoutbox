<!-- for displaying error messages -->
<% if (locals.messages) { %>
    <% messages.forEach(message => { %>
        <p class="<%= message.type %>"><%= message.string %></p>
    <% }) %>
    <% removeMessages() %>
<% } %>